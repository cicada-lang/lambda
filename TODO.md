- `ThunkValue`
- `ThunkValue` -- has `cache`

- `Value` -- `equivalent`
- `Value` -- `equivalent` -- can handle recursion

- `(equal!)`
- `(not-equal!)`

- `Value` -- `normalize`
- `Value` -- `normalize` -- can handle recursion
